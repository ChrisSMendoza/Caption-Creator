
import CaptionCreator from '@/views/CaptionCreator.vue';
import { mount } from '@vue/test-utils';

describe('CaptionCreator.vue', () => {

	it('should split song lyrics into an array', () => {

		const ccWrapper = mount(CaptionCreator);
	})
});